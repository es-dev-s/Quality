export const SUPERADMIN_ROLE_SLUG = "superadmin";

/** Set to true when bulk user import is ready to ship. */
export const IMPORT_ENABLED = false;

export const PROTECTED_ROUTES = [
  "/dashboard",
  "/analytics",
  "/reports",
  "/settings",
  "/admin",
];

export const SUPERADMIN_ROUTES = ["/admin"];
